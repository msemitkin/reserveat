type: object
properties:
  startTime:
    type: string
    format: date-time
    description: Start time of the reservation
  duration:
    type: integer
    minimum: 1
    description: Duration of the reservation in minutes
required:
  - startTime
  - duration
